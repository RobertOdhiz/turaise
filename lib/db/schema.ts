// Re-export Prisma types for backward compatibility
// New code should import directly from @prisma/client

export type {
  User,
  Campaign,
  Donation,
  AuditLog,
  WithdrawalRequest,
} from "@prisma/client"
